It should compile with the provided makefilewith a simple make command. The makefile does not support any options. Command to compile:

make

The program can be run with the below command

./pa3

As far as I can tell it seems to be working. It hasn't been super extensively tested but it works on all operations. 